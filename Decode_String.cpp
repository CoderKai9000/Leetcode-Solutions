#include <bits/stdc++.h>
using namespace std;

class Solution {
public:
    string decodeString(string s) {
        stack<char> initial, temp, res;
        for(char c: s) {
            initial.push(c);
        }
        

    }
};

int main() {

    return 0;
}